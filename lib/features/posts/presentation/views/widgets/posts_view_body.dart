import 'package:flutter/material.dart';

class PostsViewBody extends StatelessWidget {
  const PostsViewBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
